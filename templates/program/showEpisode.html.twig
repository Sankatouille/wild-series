{% extends 'base.html.twig' %}

{% block title %}SeaEpisodeson #{{ episode.id }}{% endblock %}

{% block body %}
    <div class="media">
        <div class="media-body">
            <h1 class="mt-0">{{ program.title }}</h1>

            <div class="numseason">Saison :{{ season.number }}</div>

            <div class="ep">episode: {{ episode.number}}, titre de l'épisode {{ episode.title }}</div>

            <div class="synopsis"> Résumé :{{ episode.synopsis }} </div>



            <p>{{ episode.synopsis }}</p>

        </div>
    </div>

    {# <a href="{{ path('program_index') }}">
        Retour à la liste des programmes
    </a> #}
{% endblock %}